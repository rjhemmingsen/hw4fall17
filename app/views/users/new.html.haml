%h1 Sign-up

= form_tag users_path do
    = label :user, :user_id, 'User-ID'
    = text_field :user, 'user_id', :id => 'signupUser'
    = label :user, :email, 'E-Mail'
    = text_field :user, 'email', :id => 'signupEmail'
    = submit_tag 'Create my account', :id => 'signupCreate'